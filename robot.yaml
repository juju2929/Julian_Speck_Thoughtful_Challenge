# robot.yaml

robot:
  name: News Scraper Robot
  type: robocorp

variables:
  # You can define default variables here if needed
  search_phrase: ""
  news_category: ""
  num_months: 0

tasks:
  - name: Run News Scraper
    script: task.py
    workitem:
      type: json
      # You can define a default work item structure here if desired
      payload:
        search_phrase: ""
        news_category: ""
        num_months: 0
